import { defineConfig } from 'vite';
export default defineConfig({ root: '.', cacheDir: './node_modules/.vite-iron-charge-fixture', server: { host: '127.0.0.1', port: 3015, strictPort: true, hmr: false }, plugins: [] });
